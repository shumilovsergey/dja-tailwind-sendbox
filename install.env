pip install virtualenv

virtualenv env
source env/bin/activate (unix)
env\Scripts\activate (windows)
Set-ExecutionPolicy RemoteSigned (if needed)

deactivate

#pip install 

pip3 install Django
pip3 install djangorestframework
pip3 install python-dotenv
pip3 install django-cors-headers
pip3 install requests
pip3 install dataclasses-serialization
pip3 install django-tailwind
pip3 install 'django-tailwind[reload]'


#comands
django-admin startproject server .
python manage.py startapp api
python manage.py makemigrations <name>
python manage.py migrate
python manage.py createsuperuser 
python manage.py runserver 0.0.0.0:80

npm install tailwindcss
npx tailwindcss init -p



# To use Tailwind CSS in a Django project, you can follow these steps:

# 1. Install Tailwind CSS and its dependencies using npm
npm install tailwindcss postcss autoprefixer

# 2. Create a Tailwind CSS configuration file
npx tailwindcss init

# 3. Configure PostCSS to process your CSS files
# Create a postcss.config.js file in your project root directory with the following content:
# module.exports = {
#   plugins: [
#     require('tailwindcss'),
#     require('autoprefixer'),
#   ]
# };

# 4. Include Tailwind CSS in your CSS files
# Create a CSS file (e.g., styles.css) and include Tailwind directives:
# @tailwind base;
# @tailwind components;
# @tailwind utilities;

# 5. Add the CSS file to your Django project
# You can include the CSS file in your Django templates using the static template tag.

# 6. Build your CSS file
# Run the following command to build your CSS file with Tailwind CSS:
npx postcss styles.css -o output.css

# 7. Link the built CSS file in your Django templates
# Include the built CSS file in your Django templates using the static template tag.